<form method="post" action="{{ path('app_quickcheck_result') }}">
    <div class="container">

        <div class="row">
            <label for="comment">Raw Template:</label>
            <textarea name="template" class="form-control" rows="30" id="comment">{{ template }}</textarea>
        </div>

        <div class="row" style="padding-top: 5px">
            <input type="submit" class="btn btn-primary" value="Process">
        </div>


    </div>
</form>